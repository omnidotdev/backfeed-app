import { beforeEach, describe, it } from "bun:test";

describe.todo("invite member", () => {
  beforeEach(() => {
    // render(<InviteMember />);
  });

  it.todo("enters emails to invite");

  it.todo("allows pasting up to max number of invites");

  it.todo("shows error if duplicate email typed or pasted");

  it.todo("shows error if more than max number of invites pasted");
});
