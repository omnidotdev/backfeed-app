fragment Project on Project {
  createdAt
  description
  id
  image
  name
  organizationId
  rowId
  slug
  updatedAt
  posts {
    nodes {
      createdAt
      description
      id
      projectId
      rowId
      title
      updatedAt
      userId
    }
  }
}
