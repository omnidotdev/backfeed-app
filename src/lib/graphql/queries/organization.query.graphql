query Organization($rowId: UUID!) {
  organization(rowId: $rowId) {
    rowId
    name
    projects {
      nodes {
        rowId
        name
        description
        posts {
          totalCount
          aggregates {
            distinctCount {
              userId
            }
          }
        }
      }
    }
  }
}
