query Organization($rowId: UUID!) {
  organization(rowId: $rowId) {
    rowId
    name
    userOrganizations {
      totalCount
    }
    projects {
      nodes {
        name
        description
        posts {
          totalCount
        }
      }
    }
  }
}
