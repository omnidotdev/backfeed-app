query Organization($slug: String!) {
  organizationBySlug(slug: $slug) {
    rowId
    name
    slug
    updatedAt
    projects(first: 6, orderBy: POSTS_COUNT_DESC) {
      nodes {
        rowId
        name
        description
        slug
        posts {
          totalCount
          aggregates {
            distinctCount {
              userId
            }
          }
        }
      }
    }
  }
}
