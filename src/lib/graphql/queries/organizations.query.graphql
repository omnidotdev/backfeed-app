query Organizations($first: Int, $orderBy: [OrganizationOrderBy!]) {
  organizations(first: $first, orderBy: $orderBy) {
    nodes {
      rowId
      name
      slug
      projects {
        totalCount
      }
      userOrganizations {
        totalCount
      }
    }
  }
}
