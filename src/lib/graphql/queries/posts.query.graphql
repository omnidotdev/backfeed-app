query Posts($projectId: Int!) {
  posts(filter: { projectId: { equalTo: $projectId } }) {
    nodes {
      rowId
      createdAt
      title
      description
      user {
        walletAddress
      }
      upvotes {
        nodes {
          rowId
        }
      }
    }
  }
}
