query Posts($projectId: Int!) {
  allPosts(filter: { projectId: { equalTo: $projectId } }) {
    nodes {
      rowId
      createdAt
      title
      description
      userByUserId {
        walletAddress
      }
      upvotesByPostId {
        nodes {
          rowId
        }
      }
    }
  }
}
