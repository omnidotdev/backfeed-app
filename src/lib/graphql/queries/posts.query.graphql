query Posts($projectId: String!) {
  allPosts(filter: { projectId: { equalTo: $projectId } }) {
    nodes {
      rowId
      createdAt
      title
      description
      userByAuthorId {
        walletAddress
      }
      upvotesByPostId {
        nodes {
          rowId
        }
      }
    }
  }
}
