query Project($organizationId: Int, $projectSlug: String!) {
  allProjects(
    filter: {
      and: {
        organizationId: { equalTo: $organizationId }
        slug: { equalTo: $projectSlug }
      }
    }
  ) {
    nodes {
      rowId
      name
      image
      description
    }
  }
}
