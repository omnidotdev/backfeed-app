query StatusBreakdown($projectId: UUID!) {
  project(rowId: $projectId) {
    posts {
      nodes {
        status {
          status
        }
      }
    }
    postStatuses {
      nodes {
        status
      }
    }
  }
}
