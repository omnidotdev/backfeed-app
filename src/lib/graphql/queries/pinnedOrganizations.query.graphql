query PinnedOrganizations {
  organizations(first: 3, orderBy: USER_ORGANIZATIONS_COUNT_DESC) {
    nodes {
      rowId
      name
      slug
      projects {
        totalCount
      }
      userOrganizations {
        totalCount
      }
    }
  }
}
