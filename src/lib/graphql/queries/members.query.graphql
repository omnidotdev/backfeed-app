query Members($organizationId: UUID!, $roles: [Role!], $username: String) {
  members(
    condition: { organizationId: $organizationId }
    filter: {
      role: { in: $roles }
      user: { username: { includesInsensitive: $username } }
    }
  ) {
    nodes {
      ...Member
    }
  }
}
